# Hello, World! Assignment

Create a C program that prints "Hello, World!"

Also create a `Makefile` to build the program.

## Starting point

Start from the folowing skeleton:

```c=
#include <stdio.h>

int main(void){
    ...
}
```

## Build
Build the program using:

```
make
```

## Test

To test, run:

```console
./helloworld | grep 'Hello, World!'
```

## Bonus: Python

As bonus, create a similar program in Python.

Start from the following skeleton:

```py=
import sys

def main():
    ...        # Add your code here.

if __name__ == "__main__":
    sys.exit(main())
```# cdl-03-demo-markdown
Demo for session 03 of CDL Summer 2026
